<UserControl x:Class="CrosswordsPuzzleGenerator.Views.CWSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrosswordsPuzzleGenerator.Views" 
             xmlns:viewmodels="clr-namespace:CrosswordsPuzzleGenerator.ViewModels" 
             xmlns:val="clr-namespace:CrosswordsPuzzleGenerator.Utilities.Validation"
             d:DataContext="{d:DesignInstance Type=viewmodels:CWSettingViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="120" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" 
                    VerticalAlignment="Top" 
                    HorizontalAlignment="Left"
                    Width="120">
            <Label Content="Puzzle Size:" />
            <TextBox Height="30" Style="{StaticResource ValidatedTextBox}">
                <TextBox.Text>
                    <Binding Path="GridSize"
                             UpdateSourceTrigger="LostFocus"
                             Mode="TwoWay"
                             ValidatesOnDataErrors="True"
                             NotifyOnValidationError="True" >
                        <Binding.ValidationRules>
                            <val:PositiveIntegerRule />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1" VerticalAlignment="Bottom"
                    Width="20" HorizontalAlignment="Left">
            <Button Height="15" Content="+" Padding="0" FontSize="10"
                    Command="{Binding IncreaseGridSizeCommand}"/>
            <Button Height="15" Content="-" Padding="0" FontSize="10"
                    Command="{Binding DecreaseGridSizeCommand}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" 
            VerticalAlignment="Top" 
            HorizontalAlignment="Left"
            MinWidth="120">
            <Label Content="Words to fit:" />
            <TextBox Height="30"
                     Style="{StaticResource ValidatedTextBox}"
                     Text="{Binding WordsToFit, Mode=TwoWay, UpdateSourceTrigger=LostFocus,
                            ValidatesOnDataErrors=True, NotifyOnValidationError=True}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Bottom"
            Width="20" HorizontalAlignment="Left">
            <Button Height="15" Content="+" Padding="0" FontSize="10"
                    Command="{Binding IncreaseWordsToFitCommand}"/>
            <Button Height="15" Content="-" Padding="0" FontSize="10"
                    Command="{Binding DecreaseWordsToFitCommand}"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2">
            <Label Content="Collection to pick from:"/>
            <ComboBox  Margin="0,0,0,10"
                       ItemsSource="{Binding CollectionNames, Mode=OneWay}"
                       SelectedItem="{Binding SelectedCollectionName}">
                
            </ComboBox>
        </StackPanel>
                
        <Button Grid.Column="0" Grid.Row="3" Content="Generate" FontSize="15"
                Margin="10"
                Command="{Binding GenerateCommand}"/>
    </Grid>
</UserControl>
