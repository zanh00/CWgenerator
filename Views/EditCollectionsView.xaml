<Window x:Class="CrosswordsPuzzleGenerator.Views.EditCollectionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CrosswordsPuzzleGenerator.Views"
        mc:Ignorable="d"
        Title="EditCollectionsView" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="0" >
            <Label Content="Word collections:" Margin="10"/>
            <ListBox Margin="10, 0" Width="150" HorizontalAlignment="Left">
                <ListBoxItem Content="Collection1"/>
                <ListBoxItem Content="Collection2"/>
                <ListBoxItem Content="Collection3"/>
                <ListBoxItem Content="Collection4"/>
            </ListBox>
            <StackPanel Orientation="Horizontal">
                <Button Content="Add new" Margin="10" />
                <Button Content="Remove" Margin="10" />
            </StackPanel>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="1">
            <StackPanel Orientation="Horizontal">
                <Label Content="Collection name: " Margin="0,10"/>
                <TextBox Width="200" Margin="10"/>
            </StackPanel>
            <TextBox Margin="10" Width="380" Height="300">
                
            </TextBox>
            <Button Content="Save" Margin="10" Width="50" HorizontalAlignment="Left"/>
        </StackPanel>
    </Grid>
</Window>
